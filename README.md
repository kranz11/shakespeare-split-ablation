# shakespeare-split-ablation
Same transformer, same config, two train/val splits on tiny Shakespeare: val loss 1.47 vs 1.30. The better number is the easier validation set, not the better model — the train/val gap is 3× smaller. A small study in how a split can flatter your results.
